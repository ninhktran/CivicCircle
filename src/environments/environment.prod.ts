/**
 * Environment settings for the production build of the application.
 * Includes configuration parameters used throughout the app.
 */
export const environment = {
  production: true,     // Flag indicating the app is running in production mode.
  apiUrl: 'https://api.yourdomain.com' // Base URL for the application's backend API.
};
